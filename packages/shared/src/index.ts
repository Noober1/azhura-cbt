/**
 * @azhura/shared - public entrypoint
 *
 * Re-exports domain types (and, going forward, zod schemas, the api-client and
 * the socket-client) consumed by both frontends. Import from "@azhura/shared".
 */

export * from "./types/index.ts";
