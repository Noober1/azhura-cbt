# ─── CONSOLE · admin + supervisor web app ─────────────────────────────────────
# Copy to .env.local and adjust for your environment.

# HTTP API endpoint. The backend (Elysia) runs on :3000 by default.
VITE_API_BASE_URL=http://localhost:3000/api
